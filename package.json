{
  "name": "encrypted-dns-traffic-fingerprinting",
  "version": "1.0.0",
  "private": true,
  "description": "Encrypted DNS Traffic Fingerprinting research lab and analyzer",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "frontend": "npm --prefix frontend run dev",
    "backend": "npm --prefix backend run dev",
    "start": "npm --prefix backend run start"
  }
}
